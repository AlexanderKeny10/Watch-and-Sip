const router = require('express').Router();



router.get('/', (req, res) => {
    // fetch call
    // send data
});

router.post('/:id', (req, res) => {
    // - data from waht user selected 
    // - save to database

});

module.exports = router;