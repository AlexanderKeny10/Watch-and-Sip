const router = require('express').Router();


// the user will click on a link to get top trending and witll be taken to this page 
// user serch by day and by week
// User can serach by movie tv and actor


router.get('/', (req, res) => {
    // axios call
    // send data
});

router.post('/:id', (req, res) => {
    // - data from what user selected 
    // - save to database

});

module.exports = router;